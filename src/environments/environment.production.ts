// Configuracion para PRODUCCION (cuando lo subimos a un servidor).
// "apiUrl" queda VACIO para usar rutas RELATIVAS (mismo origen). Si en el
// futuro el backend vive en otro dominio, pone aqui su direccion publica.
// Ejemplo: 'https://api.tutienda.com'
export const environment = {
  production: true,
  apiUrl: '',
};